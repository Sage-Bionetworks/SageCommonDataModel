# Example PORTAL record.
# Validate with:
#   linkml-validate -s src/sage_cdm.yaml -C Portal examples/portal.yaml
#
# The worked example from the Entity: PORTAL page. launch_date is deliberately omitted:
# the page marks it TBD for this portal, and the spec says to leave it blank rather than
# guess a date.
id: portal.amp-als
name: AMP ALS Portal
description: >-
  The AMP ALS Portal provides open access to data, tools, and analyses generated by the
  Accelerating Medicines Partnership for ALS. It serves researchers studying amyotrophic
  lateral sclerosis and hosts multi-omic data contributed by participating consortium
  members.
url: https://ampals.synapse.org
status: active
maintaining_organization: org.sage-bionetworks
source: manual curation
